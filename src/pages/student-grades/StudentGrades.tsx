import React, { ReactNode } from "react";

/**
 * Representa una lista de califiaciones del alumno.
 * @returns {ReactNode} - la pagina de calificaciones del alumno en JSX
 */

const StudentGrades = (): ReactNode => {
  return <div>Student Grades works!</div>;
};

export default StudentGrades;
