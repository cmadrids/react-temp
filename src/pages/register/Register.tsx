import React, { ReactNode } from "react";

/**
 * Represents the Register page.
 * @returns {ReactNode} - The Register JSX page.
 */
const Register = (): ReactNode => {

    return <div>Regiter works!</div>

}

export default Register;