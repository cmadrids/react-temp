import React, { ReactNode } from "react";

/**
 * Representa la pagina de inicio de sesion
 * @returns {ReactNode} - la pagina de inicio de sesion en JSX.
 */

const Login = (): ReactNode => {
  return <div>Login works!</div>;
};

export default Login;
