import React, { ReactNode } from "react";

/**
 * Represents the Topbar.
 * @returns {ReactNode} - The Topbar JSX element.
 */
const Topbar = (): ReactNode => {

    return <div>Topbar Works!</div>

}

export default Topbar;